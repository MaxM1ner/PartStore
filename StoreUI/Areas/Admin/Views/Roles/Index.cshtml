@model IEnumerable<IdentityRole>

<h2>Roles list</h2>
<table class="table">
    @foreach (var role in Model)
    {
        <tr>
            <td>@role.Name</td>
            <td>
                <form asp-action="Delete" asp-route-id="@role.Id" method="post">
                    <button type="submit" class="manage-btn btn-danger">
                        Delete
                    </button>
                </form>
            </td>
        </tr>
    }
</table>
<a class="add-btn" asp-action="Create">Add role</a>
<a class="add-btn" asp-action="CustomerList">Customers list</a>